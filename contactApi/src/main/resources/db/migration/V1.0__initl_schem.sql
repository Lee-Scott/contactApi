CREATE TABLE contact (
    id VARCHAR(255) PRIMARY KEY,
    name VARCHAR(255),
    email VARCHAR(255),
    title VARCHAR(255),
    phone VARCHAR(255),
    address VARCHAR(255),
    status VARCHAR(255),
    photo_url VARCHAR(255),
    description VARCHAR(2000)
);